// 140309u  Macquarie
// still trying to debug this

#include <stdio.h>

main ()
{
	double nc;
	int E;
	E == 'E';
	for (nc = 0; getchar() != E; ++nc)
	  {printf("%.0f ", nc); };
	  
	    printf ("doneforloop\n");
	    
		printf("%.0f\n", nc); 
		printf ("end\n");
}